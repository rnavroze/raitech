import { BbcodeParserPipe } from './bbcode-parser.pipe';

describe('BbcodeParserPipe', () => {
  it('create an instance', () => {
    const pipe = new BbcodeParserPipe();
    expect(pipe).toBeTruthy();
  });
});
